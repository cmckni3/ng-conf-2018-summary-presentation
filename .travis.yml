language: node_js
node_js:
  - "8"
cache: yarn
env:
  - BASE_HREF=/ng-conf-2018-summary-presentation/
script: yarn build
deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN
  local_dir: dist
  on:
    branch: master
